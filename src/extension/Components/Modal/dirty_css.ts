export const ModalCss = `
.darkBG {
  background-color: rgba(0, 0, 0, 0.2);
  width: 100vw;
  height: 100vh;
  z-index: 2000;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  position: absolute;
}

.centered {
  z-index: 3000;
  position: fixed;
  top: 20%;
  right: 2%;
  transform: translate(-50%, -50%);
}
`
